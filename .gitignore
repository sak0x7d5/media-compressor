.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Rust
/src-tauri/target/
/src-tauri/gen/schemas/

# Scratch encodes and downloaded tool binaries
/tmp/
*.log
ffmpeg2pass-*.log*

# Test media is generated on demand by the integration tests, never checked in —
# a fixture in the repo can drift from what the tests assume.
/tests/assets/

# Personal tool approvals, full of machine-specific absolute paths.
.claude/settings.local.json

# Personal scratch task list, deliberately not shared.
TODO.md
